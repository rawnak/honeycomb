#!/bin/bash

classes=( z-closure z-closure-marshal z-map-iter z-map z-object z-string-iter z-string z-vector-iter z-vector z-file )

for class_name in ${classes[@]}; do
   cp build/core/$class_name.[ch] bootstrap/
done


